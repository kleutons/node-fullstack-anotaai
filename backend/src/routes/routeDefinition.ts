const URL_PREFIX = '/api';

export enum ROUTERS{
    ITEM            = URL_PREFIX+'/item',
    ITENS           = URL_PREFIX+'/itens',
    USER            = URL_PREFIX+'/user',
    USERS           = URL_PREFIX+'/users',
}